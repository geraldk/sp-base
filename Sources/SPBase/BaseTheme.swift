import UIKit

struct BaseTheme {
    let baseColor = UIColor.white
    let textColor = UIColor.black
    let highlightColor = UIColor.blue
    let foreground = UIColor(named: "background")
    let background = UIColor(named: "foreground")
}
