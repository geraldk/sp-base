import UIKit

public struct BaseTheme {
    let baseColor = UIColor.white
    let textColor = UIColor.black
    let highlightColor = UIColor.blue
    let foreground = UIColor(named: "background", in: .module, compatibleWith: nil)
    let background = UIColor(named: "foreground", in: .module, compatibleWith: nil)
}
